#this is used  If main.py is outside the utils     (mostly used)
# package → import like import utils.shipping.



# import utils.shipping
# utils.shipping.calc_shipping()



# or 

# If main.py is inside the utils
# package → import just import shipping.